{
  "name": "@orqo/core",
  "version": "0.9.0",
  "description": "ORQO — Motor core multi-agente (Clean Architecture + DDD + CQRS)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/entrypoints/webhook.ts",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.31.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "mongodb": "^7.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "tsx": "^4.15.0",
    "typescript": "^5.4.5"
  }
}
